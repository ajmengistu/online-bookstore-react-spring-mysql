pipeline {
    agent any
    stages {
        stage('Back-end') {            
            steps {
                echo 'mvn --version'
            }
        }
        stage('Front-end') {            
            steps {
                echo 'node --version'
            }
        }
    }
}
